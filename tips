Modify the runall script to stop regression testing (set reg_test to 0).

These regression tests actually require that print is implemented since
the semantics of expression statements are changed from the prior assignment.
